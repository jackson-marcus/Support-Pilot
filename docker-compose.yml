services:
  api:
    build: .
    env_file: .env
    ports:
      - "8090:8000"
    volumes:
      - ./data:/app/data

  ui:
    build:
      context: .
      dockerfile: Dockerfile.ui
    environment:
      SUPPORTPILOT_API_URL: http://api:8000
    ports:
      - "8591:8501"
    depends_on:
      - api
